BACKEND_URL=http://localhost:3333
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
GOOGLE_WEB_CLIENT_ID=
GOOGLE_IOS_CLIENT_ID=

# Optional: a backend-issued JWT used by the Expo Go dev-login shortcut
# (real Google auth cannot run inside Expo Go). Leave empty to hide the button.
DEV_ACCESS_TOKEN=
