I don't think the ledge-drawer is right, the idea is when I ask the user to help with a task, the app open a screen above the chat surface with an icon and a task title

the content is only a text - but into the main component it can be just text, or a todolist, or other components in this line of thoght

the user can finish the task, and access the task history in the menu bar

we also go back to the main chat page, and we can see the amount of active tasks, and one of them, when we click we can select the task to work on

the intent of this component is to help the user on a specific task - like a todo list or write and email

the chat input we be at the bottom of the task page so the ai can performe some actions like creting or removing new todos, or just editing the text

after the ai changes the user can approve or reject the changes (like a git diff)