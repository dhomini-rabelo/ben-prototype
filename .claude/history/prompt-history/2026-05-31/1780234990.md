<ide_opened_file>The user opened the file /home/fael/so/repos/ben-prototype/.claude/skills/task-multiple-plans/SKILL.md in the IDE. This may or may not be related to the current task.</ide_opened_file>
Also add a stage 6 to implement the code using the same agent that created the plan using a prompt like:

```
Implement this plan

Make sure to follow the patterns and best practices of the codebase, using the skills `code-get-coding-designs`, `code-write-code`, and `code-most-used-libraries` to keep the code consistent with the existing codebase.
```

stage 7: review if everything is right, and give a final feeback


----

update the file to not to instruct sub agents to format files, can conflict durint the parallel job

and in the subagents prompts make them aware of the parallel job
