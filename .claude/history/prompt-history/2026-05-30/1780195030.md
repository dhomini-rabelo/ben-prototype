<ide_selection>The user selected the lines 11 to 19 from /home/fael/so/repos/ben-prototype/project-backend/src/infra/services/firebase-auth-provider.ts:
function getFirebaseAuth() {
  if (getApps().length === 0) {
    initializeApp({
      credential: cert({ projectId: env.FIREBASE_PROJECT_ID }),
    })
  }
  return getAuth()
}


This may or may not be related to the current task.</ide_selection>
@project-backend/src/infra/services/firebase-auth-provider.ts#11-19 pesquisa na internet como fazer isso daqui de forma correta. E depois corrija o código.
