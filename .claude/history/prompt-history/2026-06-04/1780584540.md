<ide_selection>The user selected the lines 32 to 48 from /home/fael/so/repos/ben-prototype/project-web/src/pages/chat/components/task-picker/active-task-picker.tsx:
  function renderContent() {
    if (state.isLoading) {
      return <TaskPickerSkeleton />;
    }
    if (state.isError) {
      return <TaskPickerError onRetry={() => actions.refetch()} />;
    }
    if (tasks.length === 0) {
      return <TaskPickerEmpty />;
    }
    return (
      <TaskPickerList
        tasks={tasks}
        onSelect={(taskId) => navigate(ROUTES.taskWorkspace(taskId))}
      />
    );
  }

This may or may not be related to the current task.</ide_selection>
i don't like functions like @project-web/src/pages/chat/components/task-picker/active-task-picker.tsx#32-48 , use the code directly in the body
