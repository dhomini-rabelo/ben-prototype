/start-office-hours-prd I want to create app of an ai friend chat bot called Ben (version 1), I thought about some features, pages and flows:

- Simple login with google (only google)
- the payment of the plan of $10, the user would have credits to spend
- we should have a single chat page 
- We should be able to set up the vibe of the agent like friendly, lovable, one that use a lot of slangs
- Chatbot page with text input (with a plus icon to attach image or any other chat option) with audio option (similar to whatsapp) at the botom
- the chatbot agent should answer fast
- at the top ben profile (his photo and name) and a hamburguer icon with management like account, plan, alarms
- we should be able to mark a message and replay an old message to have a dynamic context
- we should have dynamic ui compoents to use over the conversation in to cases:
  - get more context: ask user tools with dynamic form inputs, like the user says "Plan my trip", the ai ask: "where? (text) budget(float)"
 - components for user to manage with the ai helper: lexample ike the user has a list of customers to call, the list in the app, the user can mark the customer they han called or ask ai to mark the first 5 customers of mark all of them, or remove or add(the ai would only edit the text for this) - we would have a circle icon button above the audio button the indicate that the compoennt dynamic is active, and the hamburguer menu would this dynamic component (we need a name for this, maybe hero)
- the bot should have tools for:
  - dynamic memory management, sometimes in the conversation the bot should use the last 10 messages, sometimes 15
  - manage notes for the user in notion
  - setup alarms for the user about somthing with reminders


Ask me any question if you need using the tool `AskUserQuestion`