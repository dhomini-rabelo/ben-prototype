/task-add-coding-pattern-or-design a new pattern for use case response structure

create, update, patch, get of an specific object response -> { item }  ( can have other data returned too )
pagination -> {
  items: T[]
  hasMore: boolean
  nextCursor: string | null
}
listing -> { items: T[] }
