<ide_opened_file>The user opened the file /home/fael/so/repos/ben-prototype/project-backend/src/domain/utils/notes.ts in the IDE. This may or may not be related to the current task.</ide_opened_file>
here we should use createID instead of new ID, always do that, 

also the entity foreign key always should be an ID class, and in this case it is a string

fix all wrong id typings over the project
