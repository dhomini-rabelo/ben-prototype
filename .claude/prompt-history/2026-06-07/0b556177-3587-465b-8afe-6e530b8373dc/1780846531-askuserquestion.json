{
  "questions": [
    {
      "question": "The diff review found convention deviations (no correctness bugs). Which improvements should I apply? See diff-review-report.md for full rationale.",
      "header": "Improvements",
      "options": [
        {
          "label": "M1 — `{ item }` envelope",
          "description": "Backend route + frontend hook/type wrap counts in the standard { item } envelope like every other read endpoint. Coordinated both-side change."
        },
        {
          "label": "M2 — Extract CountBadge",
          "description": "Move the private CountBadge helper out of menu-sidebar.tsx into its own file (one-component-per-file memory rule)."
        },
        {
          "label": "M3 — Rename container to -view",
          "description": "Rename menu-sidebar-container.tsx → menu-sidebar-view.tsx / MenuSidebarView to match the codebase-wide container naming, update menu-overlay import."
        },
        {
          "label": "L1+L2 — Counts plural naming",
          "description": "Rename CapturesCountResponse → CapturesCountsResponse and useCapturesCountData → useCapturesCountsData to match the backend's plural naming."
        }
      ],
      "multiSelect": true
    }
  ],
  "answers": {
    "The diff review found convention deviations (no correctness bugs). Which improvements should I apply? See diff-review-report.md for full rationale.": "apply all improvements"
  }
}
