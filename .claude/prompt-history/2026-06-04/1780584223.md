<ide_selection>The user selected the lines 59 to 71 from /home/fael/so/repos/ben-prototype/project-web/src/pages/chat/components/task-picker/active-task-picker.tsx:
            <TaskPickerSheet
              variant={variant}
              tasks={tasks.map((task) => ({
                id: task.id,
                title: task.title,
                contentType: task.contentType,
                supporting: relativeTime(task.lastActivityAt),
              }))}
              onSelect={(taskId: string) => {
                navigate(ROUTES.taskWorkspace(taskId));
              }}
              onRetry={() => actions.refetch()}
            />

This may or may not be related to the current task.</ide_selection>
@project-web/src/pages/chat/components/task-picker/active-task-picker.tsx#59-71 esse componente gerencia muitos componentes dentro dele, com opções diferentes como:
- selecionado
- está
- vazio
- carregando
- erro
Como você me sugere que a gente pode simplificar esse componente, seguindo os padrões de código que nós temos nessa aplicação e também padrões de código em geral de React?
