<ide_selection>The user selected the lines 124 to 147 from /home/fael/so/repos/ben-prototype/project-web/src/pages/chat/states/chat-store.ts:
function animateReply(
  set: StoreSet,
  get: StoreGet,
  messageId: string,
  fullText: string,
) {
  get().stopTyping();
  let revealed = 0;
  const intervalId = setInterval(() => {
    revealed = Math.min(revealed + TYPING_CHARS_PER_STEP, fullText.length);
    const nextText = fullText.slice(0, revealed);
    set((state) => ({
      sessionMessages: state.sessionMessages.map((message) =>
        message.id === messageId
          ? { ...message, parts: [{ type: "text", text: nextText }] }
          : message,
      ),
    }));
    if (revealed >= fullText.length) {
      get().stopTyping();
    }
  }, TYPING_STEP_MS);
  set({ typingIntervalId: intervalId });
}

This may or may not be related to the current task.</ide_selection>
fix this error:

@project-web/src/pages/chat/states/chat-store.ts#124-147 Cannot invoke an object which is possibly 'undefined'.ts(2722)
⚠ Error

Cannot invoke an object which is possibly 
 .
(parameter) get: undefined
